# watchforge

## What it does
* Watches every file
* Starts every process inside a namespace
* Debounce using a timer
* Parse .gitignore file and using that build the tree
* Make a .github/workflow for automated build and release
